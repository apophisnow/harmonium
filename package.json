{
  "name": "harmonium",
  "private": true,
  "workspaces": [
    "packages/shared",
    "server",
    "client"
  ],
  "scripts": {
    "dev": "npm run dev:services && npm run build --workspace=packages/shared && concurrently -k -n shared,server,client -c blue,green,cyan \"npm run dev --workspace=packages/shared\" \"npm run dev --workspace=server\" \"npm run dev --workspace=client\"",
    "dev:services": "docker compose up postgres redis -d --wait",
    "dev:server": "npm run dev --workspace=server",
    "dev:client": "npm run dev --workspace=client",
    "build": "npm run build --workspace=packages/shared && npm run build --workspace=server && npm run build --workspace=client",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "db:push": "npm run db:push --workspace=server",
    "db:generate": "npm run db:generate --workspace=server",
    "db:migrate": "npm run db:migrate --workspace=server",
    "test": "npm run test --workspaces --if-present",
    "test:coverage": "npm run test:coverage --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
